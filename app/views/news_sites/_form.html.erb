<%= form_with(model: news_site, local: true) do |form| %>
  <% if news_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news_site.errors.count, "error") %> prohibited this news_site from being saved:</h2>

      <ul>
      <% news_site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :news_site_name %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :news_site_url %>
  </div>

  <div class="field">
    <%= form.label :rating %>
    <%= form.number_field :rating, id: :news_site_rating %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
