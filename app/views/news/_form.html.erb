<%= form_with(model: news, local: true) do |form| %>
  <% if news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news.errors.count, "error") %> prohibited this news from being saved:</h2>

      <ul>
      <% news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id, id: :news_category_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :news_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :news_description %>
  </div>

  <div class="field">
    <%= form.label :pubDate %>
    <%= form.date_select :pubDate, id: :news_pubDate %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link, id: :news_link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
