<%= form_with(model: rss_url, local: true) do |form| %>
  <% if rss_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rss_url.errors.count, "error") %> prohibited this rss_url from being saved:</h2>

      <ul>
      <% rss_url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :news_site_id %>
    <%= form.number_field :news_site_id, id: :rss_url_news_site_id %>
  </div>

  <div class="field">
    <%= form.label :category_group_id %>
    <%= form.number_field :category_group_id, id: :rss_url_category_group_id %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url, id: :rss_url_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
