<%= form_with(model: category_group, local: true) do |form| %>
  <% if category_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_group.errors.count, "error") %> prohibited this category_group from being saved:</h2>

      <ul>
      <% category_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :category_group_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
